---
version: 1
runtime: nodejs16
build:
  env:
    - name: MYVAR
      value: FOO
  commands:
    pre-build:
      - echo "------------------------------------------------pre-build_start--------------------------------------------"
      - env
      - echo "------------------------------------------------pre-build_end--------------------------------------------
    build:
      - echo "------------------------------------------------build_start--------------------------------------------"
      - env
      - npm install
      - npm run build
      - echo "------------------------------------------------build_end--------------------------------------------"
    post-build:
      - echo "------------------------------------------------post-build_start--------------------------------------------"
      - env
      - echo "------------------------------------------------post-build_end--------------------------------------------
run:
  command: npm start
  network:
    port: 3000
  env:
    - name: MYVAR
    - value: FOO
